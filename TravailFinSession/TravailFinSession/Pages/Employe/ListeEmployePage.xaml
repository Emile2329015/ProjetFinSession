<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailFinSession.Pages.Employe.ListeEmployePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailFinSession.Pages.Employe"
    xmlns:classes="using:TravailFinSession.Classes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="AliceBlue">


        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>



        <TextBlock x:Name="tblListeEmployé" Text="LISTE DES EMPLOYÉS " FontWeight="Bold"
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     FontSize="48"   Grid.Row="0"/>


        <GridView x:Name="gvListeEmploye"   Grid.Row="1">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="classes:Employe">

                    <Grid Background="Beige" Margin="10" Width="400" Height="150">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>
                        <Image Source="{x:Bind Photo}" Stretch='Fill' Height="250" Grid.Column="0" Grid.Row="0" Grid.RowSpan="4"/>

                        <TextBlock Text="{x:Bind NomComplet}"
                                     TextWrapping="Wrap" 
                                     FontWeight="Bold" 
                                     Grid.Column="1" Grid.Row="0" 
                                     TextAlignment="Left" 
                                     Foreground="CadetBlue" 
                                     FontSize="25" 
                        />

                        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" Spacing="20">

                            <TextBlock Text="{x:Bind Adresse, Mode=OneWay}"  TextAlignment="Left" Grid.Column="1" Grid.Row="1"/>
                            <TextBlock Text="{x:Bind Email, Mode=OneWay}"  TextAlignment="Center" Grid.Column="1" Grid.Row="1"/>
                            <TextBlock Text="{x:Bind Matricule, Mode=OneWay}"  TextAlignment="Left" Grid.Column="1" Grid.Row="1"/>
                            <TextBlock Text="{x:Bind StatutEmploye, Mode=OneWay}"  TextAlignment="Center" Grid.Column="1" Grid.Row="1"/>

                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="3">

                            <Button Click="Modifier_Employe"  HorizontalAlignment="Center" Background="CornflowerBlue"  >
                                <StackPanel Orientation="Horizontal" Spacing="10" >
                                    <SymbolIcon Symbol="Edit" />
                                    <TextBlock Text="Modifier" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                    </Grid>

                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>


    </Grid>
</Page>

