<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TravailFinSession.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailFinSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TravailFinSession">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar 
            Grid.Row="1" x:Name="titleBar"
            Title="GESTION DE PROJETS"
            Subtitle="Projet de fin de session"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False">
            <TitleBar.IconSource >
                <ImageIconSource ImageSource="/Assets/logoProjet.png"/>
            </TitleBar.IconSource>
            <TitleBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <MenuBar>
                        <MenuBarItem Title="MENU" Grid.Row="1">
                            <MenuFlyoutItem Text="Exporter les projets" Tag="exporter" Click="MenuFlyoutItem_Click" />
                        </MenuBarItem>

                    </MenuBar>
                    
                    <AutoSuggestBox
                    Width="500"
                    VerticalAlignment="Center"
                    PlaceholderText="rechercher des projets, employés, clients..."
                    QueryIcon="Find" />

                    
                </StackPanel>
            </TitleBar.Content>

            <TitleBar.RightHeader>
                <PersonPicture
            Width="30"
            Height="30"
            Initials="NER" />
            </TitleBar.RightHeader>
        </TitleBar>



        <MenuBar>
            <MenuBarItem Title="MENU" Grid.Row="1">
                <MenuFlyoutItem Text="Exporter les projets" Tag="exporter" Click="MenuFlyoutItem_Click" />
            </MenuBarItem>

        </MenuBar>



        <NavigationView PaneDisplayMode="Left" PaneTitle="GESTIONNAIRE PROJETS" x:Name="navView" ItemInvoked="navView_ItemInvoked" Background="LightGray" Grid.Row="2">
            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="Projet"/>
                <NavigationViewItem Content="Liste des projets" x:Name="listeProjets" Icon="List"  Tag="listeProjets"/>
                <NavigationViewItem Content="Ajouter un projet" x:Name="addMeuble" Icon="Add"  Tag="addProjet"/>
                <NavigationViewItemSeparator />
                <NavigationViewItemHeader Content="Employés"/>
                <NavigationViewItem Content="Liste des employés" x:Name="listeEmployes" Icon="Contact" Tag="listeEmployes"/>
                <NavigationViewItem Content="Ajouter un employé" x:Name="addEmploye" Icon="Add"  Tag="addEmploye"/>
                <NavigationViewItemSeparator />
                <NavigationViewItemHeader Content="Clients"/>
                <NavigationViewItem Content="Liste des Clients" x:Name="listeClients" Icon="Contact" Tag="listeClients"/>
                <NavigationViewItem Content="Ajouter un Client" x:Name="addClient" Icon="Add"  Tag="addClient"/>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Déconnexion" x:Name="iDeconnexion" Icon="Switch" Tag="deconnexion" />
                <NavigationViewItem Content="Aide" x:Name="iAide" Icon="Help" Tag="aide"/>
            </NavigationView.FooterMenuItems>
            <Frame x:Name="mainFrame" />
        </NavigationView>
    </Grid>
</Window>
